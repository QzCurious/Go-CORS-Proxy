# PAC-Based Selective Managed Proxy

Managed System Proxy uses PAC Routing instead of routing all system traffic through the gateway.

This is more complex than installing a global proxy, but it keeps unrelated traffic direct, avoids disturbing browser and OS flows such as passkeys and mobile handoff, and limits normal managed gateway routing to Domain List matches.
